import type { FastifyInstance } from "fastify";

// Vehicle compatibility routes removed (automotive feature)
export function registerProductVehicleRoutes(_app: FastifyInstance) {
  // No-op: vehicle compatibility removed
}
