import type { FastifyInstance } from "fastify";

// Customer vehicle routes removed (automotive feature)
export function registerCustomerVehicleRoutes(_app: FastifyInstance) {
  // No-op: customer vehicles removed
}
